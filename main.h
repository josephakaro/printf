#ifndef MAIN_H
#define MAIN_H

#include <unistd.h>
#include <stdio.h>
#include <stdarg.h>
#include <stdlib.h>
#include <limits.h>

extern int _putchar(char, c);
extern int _printf(const char *format, ...);

#endif
